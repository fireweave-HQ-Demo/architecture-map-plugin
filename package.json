{
  "name": "architecture-map-plugin",
  "version": "1.0.1",
  "private": true,
  "description": "Marketplace repository for the architecture-map plugin: repo surface, feature flows, plan review, and PR before/after as interactive HTML.",
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "test": "bun test ./tests",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "check": "bun run typecheck && bun run test",
    "fixtures": "bun plugins/architecture-map/skills/architecture-map/runtime/cli.ts fixtures"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "happy-dom": "^17.0.0",
    "typescript": "^5.6.0"
  }
}
