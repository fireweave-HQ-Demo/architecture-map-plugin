{
  "name": "architecture-map-plugin",
  "version": "0.0.0-dev",
  "private": true,
  "description": "Marketplace repository for the architecture-map plugin: repo surface, feature flows, plan review, and PR before/after as interactive HTML.",
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "test": "bun test ./tests",
    "typecheck": "bunx tsc -p tsconfig.json --noEmit",
    "sync": "bun tools/sync-manifests.ts",
    "sync:check": "bun tools/sync-manifests.ts --check",
    "stamp": "bun tools/stamp-version.ts",
    "check": "bun run sync:check && bun run typecheck && bun run test",
    "fixtures": "bun plugins/architecture-map/skills/architecture-map/runtime/cli.ts fixtures"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "happy-dom": "^17.0.0",
    "typescript": "^5.6.0"
  }
}
